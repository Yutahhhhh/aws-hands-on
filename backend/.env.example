# Database (ローカル開発用)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=hono_crud

# Application
NODE_ENV=development
PORT=3000

# 本番環境用（Secrets Managerから注入される）
# NODE_ENV=production
# DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
# DB_PORT=5432
# DB_NAME=your_production_db
# DB_USER=postgres  
# DB_PASSWORD=your-secret-password
# AWS X-Ray(タスク定義の環境変数から取得)
# AWS_XRAY_TRACING_NAME=your-service-name